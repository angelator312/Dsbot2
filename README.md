# Discord bot for encoding and decoding with emiojis
## Encode command: 
`/encode <string> <key>`
And returns the encoded string.
## Decode command: 
`/decode <string> <key>`
And returns the decoded string.

### Also there's joke command, but the jokes are only on Bulgarian.
